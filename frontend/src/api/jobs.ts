// src/api/jobs.ts
// TODO: fetch wrappers for job endpoints
// fetchJobs(filters) → GET /jobs
// fetchJob(id)       → GET /jobs/{id}
// searchJobs(query)  → GET /jobs/search
// saveJob(id)        → POST /jobs/{id}/save
// applyJob(id)       → POST /jobs/{id}/apply
// fetchSavedJobs()   → GET /jobs/saved
// fetchAppliedJobs() → GET /jobs/applied
