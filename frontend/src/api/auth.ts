// src/api/auth.ts
// TODO: fetch wrappers for auth endpoints
// signup(name, email, password)  → POST /auth/signup  → store JWT
// login(email, password)         → POST /auth/login   → store JWT
// updateUser(fields)             → PATCH /auth/user
// getMe()                        → GET  /auth/me
