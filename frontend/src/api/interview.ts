// src/api/interview.ts
// TODO: fetch wrappers for interview endpoints
// fetchQuestions(jobId)          → GET /interview/{job_id}/questions
// submitAnswer(jobId, answer)    → POST /interview/{job_id}/feedback
