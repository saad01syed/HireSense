# api/routers/resume.py
# TODO: Resume endpoints

# POST /resume/upload     — accept file, run analysis, save to DB, return results
# GET  /resume/latest     — get user's most recent resume + analysis (auth required)
